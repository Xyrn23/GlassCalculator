// Add the functionality to your keyboard buttons
const buttons = document.querySelectorAll(".button");

buttons.forEach((button) => {
  button.addEventListener("click", (event) => {
    // Handle button click
  });
});
